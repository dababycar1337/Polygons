using Avalonia;
using Avalonia.Controls;
using Avalonia.Media;
namespace MyApp
{
    class Square: Shape
    {
        public Square(int x, int y, string color) : base(x, y, color)
        {
        }
    }
}